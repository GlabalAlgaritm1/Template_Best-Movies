import React from 'react'
import { Element } from '../Assets/Data'

const Home = () => {
    return (
        <section className={`${Element.Container} h-screen`}>
            Home
        </section>
    )
}

export default Home